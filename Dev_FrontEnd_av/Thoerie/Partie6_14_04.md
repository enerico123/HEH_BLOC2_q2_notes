

fonction dispatch(action)

action objet -> action = {type: type d'action, payload: données}

```react 
const numbers = [1, 2, 3, 4];

const sum = numbers.reduce((acc, num) => acc + num, 0);
```

```react 
const numbers = [1, 2, 3, 4];

numbers.find(n => n > 1 ) => 2 
```

console.table(nomTableau) -> affiche sous forme de tableau

